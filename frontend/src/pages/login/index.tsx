import Header from '@/components/header';
import Footer from '@/components/footer';

const LoginPage = () => {
  return (
    <div className="min-h-screen flex flex-col">
      <Header />
      <div>로그인 페이지 구현 여기서 해주시면 됩니다.</div>
      <Footer />
    </div>
  );
};

export default LoginPage;
